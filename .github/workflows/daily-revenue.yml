name: Revenue preview (broadcast owned by bot)

on:
  workflow_dispatch:
    inputs:
      report_date:
        description: "Optional report date (YYYY-MM-DD); blank means today"
        required: false
        type: string
      dry_run:
        description: "Read connected sources and show the message without sending it"
        required: false
        default: true
        type: boolean

permissions: {}

jobs:
  broadcast:
    runs-on: ubuntu-latest
    env:
      SQUARE_ACCESS_TOKEN: ${{ secrets.SQUARE_ACCESS_TOKEN }}
      SQUARE_LOCATION_ID: ${{ secrets.SQUARE_LOCATION_ID }}
      SQUARE_TIMEZONE: Europe/London
      REVENUE_LEDGER_JSON: ${{ secrets.REVENUE_LEDGER_JSON }}
      REPORT_DATE: ${{ inputs.report_date }}
      DRY_RUN: 'true'
    steps:
      - name: Check out the report
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Preview only; never send from this workflow
        run: python scripts/revenue_summary.py
